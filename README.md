# New Tricks Homebrew tap

```bash
brew install --HEAD new-tricks/tap/newtricks   # builds from main; stable once v0.1.0 is released
tricks --help
```

The formula installs the `tricks` command from [new-tricks/tricks](https://github.com/new-tricks/tricks).
